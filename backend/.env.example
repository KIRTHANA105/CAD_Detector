# Optional environment variables for ecg_backend
# Copy to .env and adjust values as needed
PORT=5000
FLASK_DEBUG=false
# Add any other secrets like MODEL_PATH or UPLOAD_DIR if you prefer overriding defaults
